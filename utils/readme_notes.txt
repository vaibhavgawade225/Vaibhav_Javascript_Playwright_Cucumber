Notes:
- Use `npm install` to install devDependencies from package.json.
- Run `npm test` to execute the Cucumber features.
- The sample resume is a tiny placeholder PDF; replace with a real file if needed.
{
  "baseUrl": "https://opensource-demo.orangehrmlive.com/",
  "browser": "chromium",
  "headless": true,
  "timeout": 30000
}

